-- CreateTable
CREATE TABLE "Cow" (
    "id" TEXT NOT NULL,
    "ageYear" INTEGER NOT NULL,
    "ageMonth" INTEGER NOT NULL,
    "isGoat" BOOLEAN NOT NULL,
    "numOfTaonom" INTEGER NOT NULL,

    CONSTRAINT "Cow_pkey" PRIMARY KEY ("id")
);
